import { EncapsulatedClass } from './encapsulated-class';

describe('EncapsulatedClass', () => {
    it('should be defined', () => {
        expect(new EncapsulatedClass()).toBeDefined();
    });
});
